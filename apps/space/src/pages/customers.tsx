import { EmptyPlaceholder } from "@/components";

export default function Customers() {
  return (
    <EmptyPlaceholder name="Customer" title="No customers added" description="You have not added any customers. Add one below." />
  )
}
